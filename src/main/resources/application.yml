jwt:
  secret: Cizo4#l&f

server:
  port: 3001

spring:
  data:
    mongodb:
      host: localhost
      database: camisola10
      port: 27017

logging:
  level:
    org:
      springframework:
        data: DEBUG

aws:
  bucketName: camisola-backend
  accessKey: ${AWS_ACCESS_KEY}
  accessKeySecret: ${AWS_ACCESS_SECRET}
  region: eu-west-1
